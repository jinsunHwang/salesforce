<div class="slds-col slds-size_1-of-12 ">
    <lightning:select label="부서" value="{! v.mapSearchParam.deptCode }"
                      variant="label-inline" aura:id="deptSelect"
                      onchange="{! c.onChangeDept }">
        <option value="">ALL</option>
        <aura:iteration items="{! v.mapSettingValue.Comm.listMapDEPT }" var="item">
            <option value="{!item.value}">{!item.label}</option>
        </aura:iteration>
    </lightning:select>
</div>


initCollect : function(component,event,helper) {
        let action = component.get('c.initCollect');
        action.setCallback(this,function(response){
            let state = response.getState();
            if(state === 'SUCCESS') {
                let result = response.getReturnValue();
                let deptList = result.deptList || [];
                let mapSettingValueOrigin = result.mapSettingValueOrigin || {};
                mapSettingValueOrigin.Comm.listMapDEPT = highDeptList;
                component.set('v.mapSettingValueOrigin',mapSettingValueOrigin);
                let mapSettingValue = this._parse(mapSettingValueOrigin);
                component.set('v.mapSettingValue',mapSettingValue);
                component.find('deptSelect').set('v.value',component.get('v.userDeptCode'));
            } else {

            }
        });
        $A.enqueueAction(action);
}
